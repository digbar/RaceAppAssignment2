spring:
  application:
    name: gateway-service

  cloud:
    gateway:
      routes:
        - id: runner-service
          uri: lb://runner-service
          predicates:
            - Path=/api/runners/**

        - id: race-service
          uri: lb://race-service
          predicates:
            - Path=/api/races/**
